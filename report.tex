\documentclass[11pt, a4paper]{article}

\usepackage[margin=1.5cm]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{xcolor}
\usepackage[round, sort&compress]{natbib}
\usepackage{placeins}
\usepackage{subcaption}

\graphicspath{{figures/}}

\title{\textbf{Simulation-Based Inference for an Adaptive-Network SIR Model}}
\author{%
  Florian Tobias Dreyer (A0338032M) \and
  Michelle Than Yi Xuan (A0338187R) \and
  Tyler Coventry-Searle (A0339854M)
}
\date{}

\begin{document}

\maketitle
\thispagestyle{empty}

\begin{center}
  \small Code and notebooks available at
  \url{https://github.com/Florian-Dreyer/SimulationProject}
\end{center}

% ============================================================
\section{Introduction}
\label{sec:intro}
% ============================================================

Infectious diseases spread through contact networks, and the structure of those
networks is not static: individuals respond to epidemic risk by avoiding
infectious contacts.
This \emph{adaptive} behaviour feeds back into transmission dynamics in ways
that a classical well-mixed SIR model cannot capture \citep{gross2006}.

\paragraph{Model.}
We study a stochastic SIR model on an evolving network of $N=200$ agents,
initially connected as an Erdős–Rényi random graph with edge probability
$p=0.05$ and 5 randomly chosen infected nodes.
At each discrete timestep, three synchronous operations update the state:
(i) each susceptible--infected (S--I) edge transmits infection with probability
$\beta$; (ii) each infected agent recovers with probability $\gamma$; and
(iii) each S--I edge is \emph{rewired} with probability $\rho$, whereby the
susceptible node severs the risky contact and reconnects to a random agent.
We place independent uniform priors on the three unknown parameters:
$\beta \sim \mathcal{U}(0.05, 0.50)$,
$\gamma \sim \mathcal{U}(0.02, 0.20)$,
$\rho \sim \mathcal{U}(0.0, 0.8)$.

\paragraph{Data.}
The observed dataset comprises $R=40$ independent replicates, each produced
with the same fixed but unknown $(\beta,\gamma,\rho)$.
Each replicate provides three observables: the time series of infected fraction,
the time series of rewiring counts, and the final degree distribution of the
network. The underlying contact network trajectory is never observed.

\paragraph{Likelihood intractability.}
Evaluating $p(\text{data}\mid\beta,\gamma,\rho)$ exactly requires summing over
all latent network trajectories, which grows combinatorially with $N$ and time,
making classical likelihood-based inference infeasible.

\paragraph{Approach.}
\emph{Simulation-based inference} (SBI) circumvents this by replacing the
likelihood with forward simulation: given any proposed $(\beta,\gamma,\rho)$,
one can cheaply generate synthetic datasets and compare them to the observations.
In the following report we apply a hierarchy of SBI methods of increasing sophistication.

% ============================================================
\section{Summary Statistics}
\label{sec:summaries}
% ============================================================

Because acceptance probability in ABC falls exponentially with summary
dimension \citep{blum2013}, we compress the raw time series to a small
set of scalar features.
To prevent any single modality from dominating the standardised Euclidean distance
— which is parameter-free and treats each $z$-scored summary equally — we design
a \emph{balanced} set of 15 features with equal representation across the three
modalities: 5 infection, 4 rewiring, and 6 degree summaries, all averaged across
replicates.
Within each group, features are chosen for identifiability
(e.g.\ \texttt{epidemic\_duration} to separate $\beta$ from $\rho$;
cross-replicate standard deviations to capture stochastic variability;
degree quartiles and entropy to describe distribution shape).

\subsection{Identifiability diagnostics}

Projecting the $M=10\,000$ prior-simulation summaries to 2D via PCA and
colouring by parameter value (Figure~\ref{fig:pca}) reveals clear gradients
for $\gamma$, moderate structure for $\beta$, and weak but non-trivial
structure for $\rho$.
This confirms the central identifiability challenge: $\beta$ and $\rho$ are
partially confounded in aggregate data, while $\gamma$ is well-separated.
Rewiring summaries carry the independent signal needed to constrain both.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{pca_diagnostic}
  \caption{PCA diagnostic: prior simulations coloured by $\beta$, $\gamma$,
           $\rho$ (left to right). Clear gradients for $\gamma$; partial
           confounding visible in $\beta$–$\rho$ panels.}
  \label{fig:pca}
\end{figure}

\subsection{Summary-set comparison}

We evaluated the single-source sets against the balanced set at $q=1\%$
to understand which data streams carry information about which parameters.
Table~\ref{tab:sumset} reports the posterior std relative to prior std (lower = more informative).

\begin{table}[h]
\centering
\caption{Relative posterior std (std\,/\,prior std) per summary set at $q=1\%$.
         Bold marks the best single-source set for each parameter.}
\label{tab:sumset}
\begin{tabular}{lrrr}
\toprule
Set & $\beta$ & $\gamma$ & $\rho$ \\
\midrule
infection\_only & 0.377 & \textbf{0.084} & 0.854 \\
rewiring\_only  & \textbf{0.216} & 0.804 & \textbf{0.173} \\
degree\_only    & 0.924 & 0.932 & 0.483 \\
balanced (joint) & 0.479 & 0.378 & 0.262 \\
\bottomrule
\end{tabular}
\end{table}

The results reveal a clear separation: $\gamma$ is almost entirely encoded in
the infection curve (0.084), $\beta$ and $\rho$ are best identified from
rewiring summaries (0.22 and 0.17), and degree features provide only moderate
additional constraint.
No single-source set identifies all three simultaneously; the \emph{balanced}
set is the only one that constrains all three to well below the prior width.

% ============================================================
\section{Rejection ABC}
\label{sec:rabc}
% ============================================================

We drew $M=10\,000$ prior samples, computed the standardised Euclidean distance
between each simulated and observed summary vector, and accepted draws below the
1\% empirical quantile \citep{pritchard1999}, giving $\varepsilon=1.2852$ and
$n_\mathrm{acc}=100$.
The 1\% quantile balances bias against variance: tighter tolerances reduce approximation
bias but increase Monte Carlo noise; the residual bias at $q=1\%$ motivates
regression adjustment (Section~\ref{sec:regadj}), which allows a looser 5\% tolerance.
Table~\ref{tab:rabc} gives the numerical summaries; the marginal posteriors are
visible as the blue curves in Figure~\ref{fig:semiauto}.

\begin{table}[h]
\centering
\caption{Rejection ABC posterior ($q=1\%$, \emph{balanced}).}
\label{tab:rabc}
\begin{tabular}{lrrrrr}
\toprule
Param & Mean & Std & 25\% & Median & 75\% \\
\midrule
$\beta$  & 0.208 & 0.063 & 0.159 & 0.201 & 0.254 \\
$\gamma$ & 0.097 & 0.020 & 0.083 & 0.096 & 0.112 \\
$\rho$   & 0.334 & 0.061 & 0.291 & 0.332 & 0.378 \\
\bottomrule
\end{tabular}
\end{table}

$\gamma$ is best-identified (directly controls epidemic duration),
$\beta$ moderately so, and $\rho$ is hardest to constrain — consistent with
the PCA diagnostic and the positive $(\beta,\rho)$ posterior correlation:
high $\beta$ can be offset by high $\rho$ to produce a similar epidemic,
so the posterior trades off transmission rate against rewiring rate.

% ============================================================
\section{Advanced Methods}
\label{sec:advanced}
% ============================================================

\subsection{Semi-Automatic ABC \citep{fearnhead2012}}
\label{sec:semiauto}

\citet{fearnhead2012} show that the optimal ABC summaries are the
posterior means $\mathbb{E}[\theta\mid x]$, which can be approximated by
fitting Ridge regression from the balanced features to each parameter on
the pilot draws.
In-sample $R^2$ values confirm strong predictability:
$R^2_\beta=0.812$, $R^2_\gamma=0.857$, $R^2_\rho=0.979$.
Running rejection ABC in the resulting 3-dimensional projected space
($\varepsilon=0.520$, $n_\mathrm{acc}=100$) substantially tightens the
posterior for $\beta$ and provides moderate gains for $\gamma$ and $\rho$
(Figure~\ref{fig:semiauto}, Table~\ref{tab:contraction}).

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{semiauto_marginals}
  \caption{Balanced rejection ABC (blue) vs.\ semi-automatic ABC (orange),
           $q=1\%$.}
  \label{fig:semiauto}
\end{figure}

\subsection{LASSO-Based Summary Selection \citep{blum2013}}
\label{sec:lasso}

Fitting LassoCV from summaries to each parameter on the pilot simulations
retained 14/15 features for $\beta$ ($\alpha=6\times10^{-4}$) and all 15 for
$\gamma$ and $\rho$ ($\alpha=8\times10^{-4}$).
No feature is entirely redundant, so the LASSO-selected set is
effectively identical to the balanced set and yields the same posterior
(Table~\ref{tab:contraction}).

\begin{table}[h]
\centering
\caption{Posterior std and relative contraction (std/prior std) for
         rejection ABC variants at $q=1\%$.}
\label{tab:contraction}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{2}{c}{$\beta$} & \multicolumn{2}{c}{$\gamma$} & \multicolumn{2}{c}{$\rho$} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
Method & Std & Rel. & Std & Rel. & Std & Rel. \\
\midrule
Prior          & 0.130 & 1.00 & 0.052 & 1.00 & 0.231 & 1.00 \\
Balanced (15d) & 0.062 & 0.48 & 0.020 & 0.38 & 0.061 & 0.26 \\
LASSO (15d)    & 0.062 & 0.48 & 0.020 & 0.38 & 0.061 & 0.26 \\
Semi-auto (3d) & 0.023 & 0.18 & 0.017 & 0.33 & 0.051 & 0.22 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Regression Adjustment \citep{beaumont2002}}
\label{sec:regadj}

After accepting draws at a loose tolerance ($q=5\%$, $\varepsilon=1.706$,
$n_\mathrm{acc}=500$), each accepted $\theta^{(i)}$ is corrected for the
residual gap between its simulated summary $s^{(i)}$ and $s_\mathrm{obs}$:
\[
  \tilde{\theta}^{(i)}
    = \theta^{(i)} - \hat{B}^\top (s^{(i)} - s_\mathrm{obs}),
\]
where $\hat{B}$ is estimated by weighted least squares with Epanechnikov
kernel weights \citep{beaumont2002}.
Figure~\ref{fig:regadj_marginals} and Table~\ref{tab:regadj} show that
adjustment dramatically sharpens the posterior
($\sigma_\beta$: 0.099$\to$0.024, $\sigma_\gamma$: 0.033$\to$0.007,
$\sigma_\rho$: 0.087$\to$0.008).
Crucially, the adjusted posterior is nearly tolerance-invariant for
$q\lesssim10\%$, whereas the raw posterior degrades rapidly at loose tolerances.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{regadj_marginals}
  \caption{Raw ABC (blue) vs.\ regression-adjusted (orange) posteriors,
           $q=5\%$, \emph{balanced} set.}
  \label{fig:regadj_marginals}
\end{figure}

\begin{table}[h]
\centering
\caption{Raw vs.\ regression-adjusted posteriors ($q=5\%$).}
\label{tab:regadj}
\begin{tabular}{llrrrrr}
\toprule
Method & Param & Mean & Std & 5\% & Median & 95\% \\
\midrule
Raw ($q=5\%$) & $\beta$  & 0.276 & 0.099 & 0.127 & 0.272 & 0.439 \\
              & $\gamma$ & 0.111 & 0.033 & 0.066 & 0.108 & 0.169 \\
              & $\rho$   & 0.367 & 0.087 & 0.216 & 0.373 & 0.504 \\
\addlinespace
Adjusted      & $\beta$  & 0.143 & 0.024 & 0.112 & 0.138 & 0.191 \\
              & $\gamma$ & 0.079 & 0.007 & 0.069 & 0.078 & 0.092 \\
              & $\rho$   & 0.299 & 0.008 & 0.286 & 0.298 & 0.313 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Synthetic Likelihood \citep{wood2010}}
\label{sec:sl}

At each MCMC proposal $\theta$, we simulate $N_\mathrm{sim}=150$ datasets
($R_\mathrm{sim}=40$ replicates each), giving an $N_\mathrm{sim}/p$ ratio of
$10.0$ for our $p=15$ summary features, and approximate the intractable
likelihood as a multivariate Gaussian fitted to the $N_\mathrm{sim}$ summary
vectors, using Ledoit--Wolf shrinkage for the covariance
\citep{ledoitwolf2004}:
\[
  \log\hat{p}_\mathrm{SL}(s_\mathrm{obs}\mid\theta)
  = \log\mathcal{N}(s_\mathrm{obs};\,\hat{\mu}(\theta),\,\hat{\Sigma}(\theta)).
\]
This is embedded in a Metropolis--Hastings MCMC with an
\textbf{Adaptive Metropolis} (AM) proposal \citep{haario2001} that updates
the proposal covariance online after $t_0=100$ warm-up steps:
\[
  \Sigma_\mathrm{prop}(t) = \frac{2.38^2}{d}\,
  \mathrm{Cov}(\theta_1,\ldots,\theta_t) + \epsilon I,
  \quad d=3 \citep{roberts1997}.
\]
Configuration: $N_\mathrm{steps}=2\,000$, burn-in $=500$,
initialised at the regression-adjusted posterior mean
$(0.143,\, 0.079,\, 0.299)$; acceptance rate $24.0\%$, consistent with
the optimal AM target \citep{roberts1997}.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{sl_trace}
  \caption{MCMC trace plots. The chain achieves an acceptance rate of 24.0\%
           and mixes across the posterior, though the ACF decays slowly
           (thinning interval 31), resulting in ESS of 7.5–11.9.}
  \label{fig:sl_trace}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.8\textwidth]{sl_marginals}
  \caption{Synthetic likelihood MCMC posterior (green) vs.\ prior (dashed).}
  \label{fig:sl_marginals}
\end{figure}

\begin{table}[h]
\centering
\caption{Synthetic likelihood MCMC posterior (1500 post-burn-in samples,
         thinning interval 31, 49 thinned draws; acceptance rate 24.0\%).}
\label{tab:sl}
\begin{tabular}{lrrrrrr}
\toprule
Param & Mean & Std & 5\% & Median & 95\% & ESS \\
\midrule
$\beta$  & 0.153 & 0.003 & 0.148 & 0.153 & 0.158 & 7.5 \\
$\gamma$ & 0.081 & 0.002 & 0.078 & 0.081 & 0.084 & 11.9 \\
$\rho$   & 0.301 & 0.005 & 0.295 & 0.300 & 0.310 & 10.2 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{ABC-MCMC \citep{marjoram2003}}
\label{sec:abcmcmc}

At each step, a proposal $\theta^* = \theta_t + \eta$, $\eta \sim \mathcal{N}(0, \Sigma)$,
is accepted if $\rho(S(x^*), S(x_\text{obs})) \leq \varepsilon$ using $R = 40$ replicates;
$\varepsilon$ is imported from rejection ABC at the $1\%$ quantile.
We compare three summary variants: \textbf{balanced}, \textbf{LASSO-selected}, and
\textbf{semi-automatic} (with $\varepsilon$ recalibrated in the projected space).

We tune the proposal $\Sigma$ in three stages — original, adaptive $1\times$, and adaptive
$0.5\times$ \citep{roberts2009}, with the final selection being the $0.5\times$ as it restores target
acceptance rates while achieving $\hat{R} < 1.001$ for all parameters. All results
use $N = 30{,}000$ steps with burn-in $b = 1{,}000$.

The semi-automatic variant gives the tightest posteriors for $\beta$ ($\text{rel} = 0.22$) and
$\rho$ ($\text{rel} = 0.28$), but at the cost of $\gamma$ precision ($\text{rel} = 0.72$ vs
$0.39$--$0.40$ for balanced and LASSO). This is a consequence of the ridge projection optimising
jointly across parameters. As expected, balanced and LASSO perform near-identically. The $\beta$--$\rho$ correlation persists across all variants
(Table~\ref{tab:corr}: $\hat\rho=0.75$--$0.94$); fixed $\varepsilon$ MCMC cannot resolve
this confounding.

\begin{table}[h]
\centering
\caption{ABC-MCMC posterior shrinkage (adaptive $0.5\times$ proposal, $N = 30{,}000$ steps,
         burn-in $b = 1{,}000$) and split-chain Gelman--Rubin $\hat{R}$.}
\label{tab:mcmc}
\begin{tabular}{lrrrrrr}
\toprule
Variant & $\beta$ & $\gamma$ & $\rho$ & $\hat{R}_\beta$ & $\hat{R}_\gamma$ & $\hat{R}_\rho$ \\
\midrule
Balanced   & 0.48 & 0.39 & 0.29 & 1.0003 & 1.0003 & 1.0002 \\
LASSO      & 0.54 & 0.40 & 0.30 & 1.0011 & 1.0001 & 1.0000 \\
Semi-auto  & 0.22 & 0.72 & 0.28 & 1.0000 & 1.0002 & 1.0000 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{SMC-ABC \citep{sisson2007}}
\label{sec:smcabc}

ABC-MCMC's fixed $\varepsilon$ limits posterior accuracy and its serially correlated samples
reduce ESS. SMC-ABC refines $N = 100$ particles over $T = 15$ generations through a
decreasing tolerance sequence $\varepsilon_t = Q_{0.75}(\{\rho_i^{(t)}\})$ \citep{beaumont2009},
perturbing particles via $\mathcal{N}(0, \Sigma)$ and producing asymptotically independent samples.

SMC substantially improves on ABC-MCMC across all variants: $\beta$ shrinkage
improves from $0.22$ to $0.06$ and $\rho$ from $0.28$ to $0.08$ for the semi-automatic
variant, and the $\beta$--$\rho$ correlation is substantially reduced
(Table~\ref{tab:corr}: $\hat\rho=0.84$ vs $0.94$).
The $\gamma$ penalty from the ridge projection is also smaller than under MCMC
($0.18$ vs $0.72$), suggesting progressive $\varepsilon$ tightening partially recovers
lost $\gamma$ information.

\paragraph{Perturbation kernel.}
We use a Gaussian kernel $K(\theta^* \mid \theta_i) = \mathcal{N}(\theta^*;\,\theta_i,\,\Sigma)$
\citep{beaumont2009} with per-component scales derived from the ABC-MCMC marginal
standard deviations via the Roberts--Rosenthal scaling rule with a halving factor,
\[
  \sigma_j = \tfrac{1}{2}\cdot\frac{2.38}{\sqrt{d}}\,\hat\sigma^\mathrm{MCMC}_j,
\]
giving $(\sigma_\beta,\sigma_\gamma,\sigma_\rho)=(0.0428,0.0148,0.0445)$ for the
balanced variant ($d=3$).
The halving reduced weight degeneracy compared with the $1\times$ scale.
A diagonal rather than full covariance was used because the off-diagonal terms
destabilised acceptance rates at $N=100$ particles.

\subsection{Synthetic Truth Recovery}
\label{sec:synthetic_truth}

\paragraph{Single-point recovery.}
To check whether the methods can recover parameters from a controlled data set,
we generated synthetic observed data ($R=40$ replicates) at
$\theta_\mathrm{true}=(\beta,\gamma,\rho)=(0.17,\,0.086,\,0.31)$
(the SMC-ABC balanced posterior mean, representing a plausible interior point)
and re-ran regression adjustment and synthetic likelihood on those data.
Regression adjustment reused the existing 10\,000-draw prior pool without additional
simulator calls; synthetic likelihood used the same configuration as the main analysis
($N_\mathrm{sim}=150$, $N_\mathrm{steps}=2{,}000$, $\mathrm{burn{-}in}=500$).

Both methods covered all three parameters in their 90\% credible intervals
(Table~\ref{tab:synthetic_truth}; Figure~\ref{fig:synthetic_truth}).
Regression adjustment gave wider intervals consistent with its $q=5\%$ tolerance;
synthetic likelihood produced much tighter but still valid intervals.

\paragraph{Simulation-Based Calibration for regression adjustment.}
A single recovery check only probes one point in parameter space.
\emph{Simulation-Based Calibration} (SBC; \citep{talts2018}) provides a stronger
test: draw $N=100$ parameter vectors $\theta^\ast$ from the prior, generate $R=40$
synthetic replicates at each, run regression adjustment, and verify that empirical
coverage matches the nominal level $\alpha$ for all $\alpha$.
Because regression adjustment reuses the existing prior pool, the only additional
cost is $100\times 40 = 4{,}000$ simulator runs.

Figure~\ref{fig:sbc_ra} shows the calibration curves.
At the 90\% nominal level, empirical coverage is 93\%, 98\%, and 95\% for
$\beta$, $\gamma$, and $\rho$ respectively, and tracks closely across all
tested levels (50\%, 80\%, 90\%, 95\%), confirming well-calibrated posteriors
throughout the prior.

Full SBC is \textbf{not} applied to synthetic likelihood: at ${\approx}12\mathrm{M}$
simulator calls per chain, 100 repetitions are infeasible.  The single-point recovery result above
(all three 90\% CIs cover $\theta_\mathrm{true}$) serves as a targeted sanity
check for SL instead.

\begin{table}[h]
\centering
\caption{Synthetic truth recovery at $\theta_\mathrm{true}=(0.17,\,0.086,\,0.31)$.
         90\% credible intervals and coverage for regression adjustment (RA)
         and synthetic likelihood (SL).}
\label{tab:synthetic_truth}
\begin{tabular}{llccccc}
\toprule
Method & Parameter & True & Mean & 90\% CI & In 90\% CI \\
\midrule
\multicolumn{6}{l}{\textbf{Regression adjustment}} \\
  & $\beta$  & 0.170 & 0.177 & $[0.145,\,0.231]$ & Yes \\
  & $\gamma$ & 0.086 & 0.086 & $[0.076,\,0.098]$ & Yes \\
  & $\rho$   & 0.310 & 0.314 & $[0.303,\,0.327]$ & Yes \\
\addlinespace
\multicolumn{6}{l}{\textbf{Synthetic likelihood} ($N_\mathrm{sim}=150$, accept.\ 28\%, 94 draws)} \\
  & $\beta$  & 0.170 & 0.176 & $[0.170,\,0.183]$ & Yes \\
  & $\gamma$ & 0.086 & 0.087 & $[0.084,\,0.090]$ & Yes \\
  & $\rho$   & 0.310 & 0.313 & $[0.305,\,0.321]$ & Yes \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\textwidth]{synthetic_truth_coverage}
  \caption{Single-point synthetic truth recovery at
           $\theta_\mathrm{true}=(0.17,\,0.086,\,0.31)$ (red dashed lines).
           Shaded bands show the 90\% credible interval; solid curves are
           posterior KDEs.
           Tick labels in each panel title indicate whether the 90\% CI covers
           the truth for regression adjustment (RA) and synthetic likelihood (SL).}
  \label{fig:synthetic_truth}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.5\textwidth]{sbc_ra_coverage}
  \caption{SBC calibration curves for regression adjustment ($N=100$ draws from the prior).
           Each point shows empirical coverage vs.\ nominal level for one parameter.
           The dashed diagonal is the ideal line; points close to it indicate
           well-calibrated credible intervals.}
  \label{fig:sbc_ra}
\end{figure}

% ============================================================
\section{Comparison}
\label{sec:comparison}
% ============================================================

Table~\ref{tab:compare} summarises all methods.
Point estimates are consistent across advanced methods
($\hat\beta\approx0.14$–$0.15$, $\hat\gamma\approx0.079$–$0.081$,
$\hat\rho\approx0.299$–$0.301$).
Regression adjustment gives the most reliable uncertainty quantification:
large accepted set ($n=500$), tolerance-robust, and verifiable via
sensitivity analysis.
The SL posterior is the sharpest; synthetic truth recovery
(Section~\ref{sec:synthetic_truth}) confirms the intervals are calibrated,
though low ESS (7.5–11.9) makes the quoted standard deviations imprecise.
ABC-MCMC at fixed $\varepsilon$ achieves shrinkage comparable to rejection
ABC, confirming that the tolerance rather than the sampler is the binding
constraint; the $\beta$--$\rho$ correlation is preserved throughout.
SMC-ABC resolves this by tightening $\varepsilon$ iteratively, reaching
0.17/0.15/0.11 with the balanced set and 0.06/0.18/0.08 with the
semi-automatic variant — a $3\times$ improvement over ABC-MCMC on all
parameters and a marked reduction in $\beta$--$\rho$ confounding
(Table~\ref{tab:corr}).
\begin{table}[h]
\centering
\caption{Posterior standard deviations across all methods.
         Values in parentheses are shrinkage = posterior std / prior std.
         $^\ddagger$SL intervals are calibrated (Section~\ref{sec:synthetic_truth}) but low ESS (7.5--11.9) makes the quoted std imprecise.}
\label{tab:compare}
\begin{tabular}{lrrrl}
\toprule
Method & $\sigma_\beta$ & $\sigma_\gamma$ & $\sigma_\rho$ & Notes \\
\midrule
Prior                        & 0.130 & 0.052 & 0.231 & -- \\
\addlinespace
\multicolumn{5}{l}{\textbf{Rejection ABC}} \\
Balanced ($q=1\%$)           & 0.062 & 0.020 & 0.061 & $n=100$ \\
LASSO ($q=1\%$)              & 0.062 & 0.020 & 0.061 & All 15 features kept \\
Semi-auto ($q=1\%$)          & 0.023 & 0.017 & 0.051 & 3-d projection \\
\addlinespace
\multicolumn{5}{l}{\textbf{Regression adjustment}} \\
Balanced ($q=5\%$)           & 0.024 & 0.007 & 0.008 & $n=500$, robust \\
\addlinespace
\multicolumn{5}{l}{\textbf{Synthetic likelihood$^\ddagger$}} \\
Balanced                     & 0.003 & 0.002 & 0.005 & $N_\mathrm{sim}=150$, accept.\ 24\% \\
\addlinespace
\multicolumn{5}{l}{\textbf{ABC-MCMC (adaptive $0.5\times$, $N=30{,}000$)}} \\
Balanced   & 0.0626 (0.48) & 0.0204 (0.39) & 0.0665 (0.29) & $\hat{R}<1.001$ \\
LASSO      & 0.0700 (0.54) & 0.0206 (0.40) & 0.0691 (0.30) & $\hat{R}<1.001$ \\
Semi-auto  & 0.0281 (0.22) & 0.0376 (0.72) & 0.0637 (0.28) & $\hat{R}<1.001$ \\
\addlinespace
\multicolumn{5}{l}{\textbf{SMC-ABC (refined $0.5\times$, $T=15$, $N=100$)}} \\
Balanced   & 0.0216 (0.17) & 0.0078 (0.15) & 0.0250 (0.11) & \\
LASSO      & 0.0238 (0.18) & 0.0086 (0.16) & 0.0286 (0.12) & \\
Semi-auto  & 0.0082 (0.06) & 0.0091 (0.18) & 0.0187 (0.08) & \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Simulator Budget}
\label{sec:budget}

Table~\ref{tab:budget} breaks down simulator calls per method.
Rejection ABC, semi-automatic ABC, LASSO ABC, and regression adjustment all
share the same prior pool and incur no additional cost, so comparisons among
them are like-for-like.
Cross-family comparisons are not budget-neutral: the superior sharpness of
SMC-ABC and synthetic likelihood partly reflects their substantially larger
simulator investment.

\begin{table}[h]
\centering
\caption{Simulator call budget.
         One ``individual run'' = one call to \texttt{simulate\_fast()}
         ($N=200$ agents, $T=200$ steps).
         The prior pool (400\,000 runs) is shared by all pool-based methods.
         $^\dagger$SMC budget is a lower bound; actual cost depends on
         per-generation acceptance rates.
         $^\ddagger$Each SL step evaluates $N_\mathrm{sim}=150$ datasets of
         $R_\mathrm{sim}=40$ replicates, giving 6\,000 simulator calls per
         MCMC step.}
\label{tab:budget}
\begin{tabular}{lrr}
\toprule
Method & Additional runs & Cumulative total \\
\midrule
Prior pool (shared by pool-based methods)        & 400\,000          & 400\,000            \\
Rejection ABC / Semi-auto / LASSO / Reg.\ adj.\  & 0                 & 400\,000            \\
ABC-MCMC (3 variants $\times$ 30\,000 steps $\times$ 40 reps)
                                                 & 3\,600\,000       & 4\,000\,000         \\
SMC-ABC (3 variants, ${\geq}15$ gens $\times$ 100 particles $\times$ 40 reps)$^\dagger$
                                                 & ${\geq}540{,}000$ & ${\geq}4{,}540{,}000$ \\
Synthetic likelihood (2\,001 steps $\times$ 6\,000 runs/step)$^\ddagger$
                                                 & 12\,006\,000      & ${\geq}16{,}546{,}000$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Posterior Correlation}
\label{sec:correlation}

Table~\ref{tab:corr} reports the Pearson correlation
$\hat\rho(\beta,\rho)$ computed from each method's posterior draws.
Regression adjustment shows the weakest correlation ($0.35$), consistent
with the linear correction partially disentangling the two parameters.
SMC-ABC balanced and ABC-MCMC balanced are similar (${\approx}0.75$--$0.76$),
confirming that iterative $\varepsilon$ tightening reduces confounding but
does not eliminate it; the semi-automatic variants push the correlation
higher ($0.84$--$0.94$) because projecting onto three dimensions discards the
rewiring and degree features that carry independent $\rho$ signal.

\begin{table}[h]
\centering
\caption{Pearson correlation $\hat\rho(\beta,\rho)$ for each posterior.
         Higher values indicate stronger $\beta$--$\rho$ confounding.}
\label{tab:corr}
\begin{tabular}{lrr}
\toprule
Method & $\hat\rho(\beta,\rho)$ & $n$ \\
\midrule
Rejection ABC              & 0.77  & 100     \\
Regression adjustment      & 0.35  & 500     \\
Synthetic likelihood        & 0.78  & 84      \\
\addlinespace
ABC-MCMC balanced          & 0.75  & 29\,000 \\
ABC-MCMC LASSO             & 0.80  & 29\,000 \\
ABC-MCMC semi-auto         & 0.94  & 29\,000 \\
\addlinespace
SMC balanced               & 0.76  & 100     \\
SMC LASSO                  & 0.78  & 100     \\
SMC semi-auto              & 0.84  & 100     \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \begin{subfigure}{0.425\textwidth}
    \includegraphics[width=\textwidth]{comparison_all_methods_marginals}
    \caption{All methods including synthetic likelihood.}
    \label{fig:marginals_all}
  \end{subfigure}
  \hfill
  \begin{subfigure}{0.425\textwidth}
    \includegraphics[width=\textwidth]{comparison_all_methods_marginals_xsl}
    \caption{All methods excluding synthetic likelihood.}
    \label{fig:marginals_xsl}
  \end{subfigure}
  \caption{Marginal posteriors (KDE) across all methods. Synthetic likelihood
           is shown separately as its extreme tightness compresses the scale
           for other methods.}
  \label{fig:marginals_comparison}
\end{figure}

\subsection{Posterior Predictive Check}
\label{sec:ppc}

Figure~\ref{fig:ppc} overlays 5th--95th percentile predictive bands on all three observed
summaries for the SMC-ABC balanced posterior.
The observed mean lies within the band throughout, confirming that the posterior is
well-specified and not overconfident.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\textwidth]{ppc_smc_balanced}
  \caption{Posterior predictive check using SMC-ABC balanced (50 draws from the
           final generation, 40 replicates each).
           Shaded bands: 5th--95th percentile of per-draw replicate means.
           Blue line: predictive mean.  Red dashed line: observed mean over 40
           replicates.  Grey traces: individual observed replicates.
           Left: infected fraction; centre: rewiring events; right: final degree
           distribution (normalised, bars = observed mean, line = predictive mean).}
  \label{fig:ppc}
\end{figure}

% ============================================================
\section{Conclusion}
\label{sec:conclusion}
% ============================================================

We have applied a progressive hierarchy of simulation-based inference methods
to recover the three parameters $(\beta, \gamma, \rho)$ of an adaptive-network
SIR model from $R=40$ replicate observations.

\paragraph{Identifiability.}
The clearest finding is that the three parameters are not equally identifiable
from aggregate data.
$\gamma$ is the best-constrained: it directly controls epidemic duration and
is strongly encoded in the infection time series, allowing even rejection ABC
to shrink its posterior to 38\% of the prior width.
$\beta$ and $\rho$ are harder to separate because both modulate epidemic size
through different mechanisms — higher transmission and lower rewiring produce
similar infection curves — leaving a genuine positive posterior correlation
between them that persists across all methods and summary sets.
Rewiring summaries carry the independent signal needed to partially resolve
this confounding; infection-curve summaries alone are essentially uninformative
about $\rho$, and this structure is intrinsic to the observables rather than a sampler artefact.

\paragraph{Method comparison.}
Each method addresses a specific limitation of its predecessor.
Semi-automatic ABC reduces summary dimensionality via ridge projection, tightening
$\beta$ and $\rho$ at the cost of $\gamma$ precision.
Regression adjustment \citep{beaumont2002} dramatically sharpens all posteriors
($\sigma_\rho$: 0.087$\to$0.008) and is robust to the choice of tolerance for $q\lesssim10\%$.
ABC-MCMC \citep{marjoram2003} achieves well-converged chains ($\hat{R}<1.001$) but its fixed
$\varepsilon$ leaves the $\beta$--$\rho$ confounding unresolved.
SMC-ABC \citep{sisson2007} delivers the best overall performance, with the semi-automatic
variant reaching shrinkage of 0.06 for $\beta$ and 0.08 for $\rho$.
Synthetic likelihood \citep{wood2010} yields the numerically sharpest posterior; synthetic
truth recovery confirms the intervals are calibrated, though the low ESS (7.5--11.9) introduces
uncertainty in the reported standard deviations.

\paragraph{Limitations and future work.}
SBC further confirms regression adjustment is well-calibrated across the full prior,
not just at a single point.
The low SL ESS could be raised by longer runs or a more targeted initialisation
without materially changing the posterior location.
For SMC-ABC, increasing the particle count beyond $N=100$ would reduce Monte
Carlo noise in the final generation and give more stable shrinkage estimates.
More broadly, the $\beta$--$\rho$ confounding is ultimately a limitation of
the aggregate summary statistics: individual-level contact tracing data, if
available, would provide direct information about rewiring events and likely
resolve the degeneracy.
Finally, neural simulation-based inference methods \citep[e.g.][]{cranmer2020}
that learn flexible approximations to the posterior directly from simulations
represent a natural next step, bypassing the need for hand-crafted summary
statistics altogether.

% ============================================================
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}